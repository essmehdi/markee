import { DialogContent } from "@/components/ui/dialog";
import { Vault } from "@/lib/vaults/types";

type SaveAsDialogProps = {
	vault: Vault;
};

export default function SaveAsDialog({ vault }: SaveAsDialogProps) {
	return <DialogContent></DialogContent>;
}
